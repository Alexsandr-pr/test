
import "./ParentFromReplace.scss"

const ParentFromReplace = ({children}) => {
    return(
        <div className="parent-from-replace">
            {children}
        </div>
    )
}

export default ParentFromReplace